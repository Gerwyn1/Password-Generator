import React, { useState, useRef } from "react";
import Head from "next/head";
import { Box, FormGroup } from "@mui/material";

import {
  Background,
  Container,
  PasswordHeader,
  PasswordBody,
} from "../src/styled/styled";
import {
  Title,
  Password,
  Slide,
  CharacterRow,
  FormRow,
  Strength,
  CopyIcon,
  GenerateButton,
} from "../src/components";

export default function Home() {
  const [password, setPassword] = useState("P4$5W0rD!");
  const placeholder = useRef();
  return (
    <Box sx={Background}>
      <Head>
        <title>Password Generator</title>
        <meta name="description" content="Generated by create next app" />
      </Head>
      <Title />
      <Box sx={Container}>
        <PasswordHeader>
          <Password password={password} />
          <CopyIcon />
        </PasswordHeader>
        <PasswordBody>
          <CharacterRow />
          <Slide />
          <FormGroup sx={{ width: "100%" }}>
            <FormRow value="Include Uppercase letters" />
            <FormRow value="Include Lowercase letters" />
            <FormRow value="Include Numbers" />
            <FormRow value="Include Symbols" />
          </FormGroup>
          <Strength setPassword={setPassword} placeholder={placeholder} />
          <GenerateButton placeholder={placeholder} />
        </PasswordBody>
      </Box>
    </Box>
  );
}
